# <Topic> — <date>

## What I predicted
<expected bytes changed / offset / test count — write BEFORE running>

## What actually happened
<command + actual output>

## Why (mechanism)
<explanation with file:line refs>

## The invariant this protects
<e.g., rom.py offsets, append-only struct rule, build determinism>

## Next experiment
